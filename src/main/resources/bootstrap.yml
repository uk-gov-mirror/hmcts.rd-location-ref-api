spring:
  cloud:
    propertiesvolume:
      prefixed: true
      paths: /mnt/secrets/s2s,/mnt/secrets/rd
      aliases:
        s2s.microservicekey-rd-location-ref-api: S2S_SECRET
        rd.location-ref-api-POSTGRES-PASS: POSTGRES_PASSWORD
        rd.AppInsightsInstrumentationKey: azure.application-insights.instrumentation-key